stages:
- name: dockerBuild
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: gitbook-util
      pushRemote: true
      registry: core.harbor.ixx
timeout: 60
notification: {}
